# :fooda: items under $<%= '%.2f' % budget.to_s %> for <%= date %>

## Main dishes recommendations

<% main_dishes.each do |item| %>
- <%= "[**#{item.name}** from **#{item.restaurant}** _($#{'%.2f' % recommendation.price.to_f.to_s})_](#{item.url})" %>
<% end %>

## Side dishes recommendations

<% side_dishes.each do |item| %>
- <%= "[**#{item.name}** from **#{item.restaurant}** _($#{'%.2f' % recommendation.price.to_f.to_s})_](#{item.url})" %>
<% end %>

<% if price_changes&.any? %>
## Price Changes

<% price_changes.each do |restaurant, changes| %>
**_<%= restaurant %>_** changed prices on some items today:

<% changes.each do |change| %>
- **_<%= change[:name] %>_** went from **<%= "$#{'%.2f' % change[:old_price]}" %> to <%= "$#{'%.2f' % change[:new_price]}" %>**
<% end %>

<% end %>

<% end %>

_Generated by v1 engine._
