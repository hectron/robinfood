# :fooda: items Under $<%= '%.2f' % budget.to_s %> for <%= date.strftime('%A %B %-d, %Y') %>

## Most expensive items

<% most_expensive_items.each do |item| %>
- <%= "[**#{item.name}** from **#{item.restaurant}** _($#{'%.2f' % item.price.to_s})_](#{item.url})" %>
<% end %>

_These are items that will maximize your stipend, sorted by most expensive descending._

<% recommendations.each_with_index do |recommendation_set, index| %>
## <%= "Idea #{index + 1}" %>

<% recommendation_set.each do |recommendation| %>
- <%= "[**#{recommendation.name}** from **#{recommendation.restaurant}** _($#{'%.2f' % recommendation.price.to_s})_](#{recommendation.url})" %>
<% end %>

_Estimated cost before tax: **$<%= '%.2f' % recommendation_set.map(&:price).reduce(:+).to_s %>**_
<% end %>

<% if price_changes&.any? %>
## Price Changes

<% price_changes.each do |restaurant, changes| %>
**_<%= restaurant %>_** changed prices on some items today:

<% changes.each do |change| %>
- **_<%= change[:name] %>_** went from **<%= "$#{'%.2f' % change[:old_price]}" %> to <%= "$#{'%.2f' % change[:new_price]}" %>**
<% end %>

<% end %>

<% end %>

_Generated by v2 engine._
