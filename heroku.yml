# Used to deploy the Docker image to Heroku
setup:
  addons:
    - plan: heroku-postgresql
build:
  config:
    RAILS_ENV: production
  docker:
    web:    Dockerfile
    worker: Dockerfile
run:
  web:    prod_web
  worker: sidekiq
