# Used to deploy the Docker image to Heroku
setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis
build:
  config:
    RAILS_ENV: production
  docker:
    web:    Dockerfile
    worker: Dockerfile
run:
  web:    ./bin/entrypoint.sh prod_web
  worker: ./bin/entrypoint.sh sidekiq
